Verification complete. Every factual claim in the critique checked out against the code (EditorTimeline is live via `AnimationEditorPanel.ts:352-353/718-780`; `Component` has no `dispose`; `AnimationStateMachine.serialize/deserialize` exist at `:359-381`; 10 tabs; 5 extra `<canvas>` in the Scene tab; no `__bf` global; 11 corrupted files plus 2 to inspect; 72 `removeEventListener`; templates return closures with `resetState` at `:1127/:1532/:3534`; `EventBus.off` at `:38`; 7 of 11 template tests die; `generateCollisionMesh` has two callers; `PrefabManager.ts`/`SaveManager.ts` are Engine-only facades not mentioned by the plan). Below is the final plan.

# Plano final — Rework da BlindFake Engine (versão final, pós-crítica)

**Base.** Plano C-risco com enxertos de A e B (inalterado). Esta versão incorpora a crítica de completude: 17 de 18 itens de "missing", 11 de 11 contradições, 9 de 9 problemas de sequência e 13 de 13 itens não verificáveis foram aceitos e absorvidos no texto; os rejeitados estão na última linha. Fatos reconfirmados no código nesta rodada: `EditorTimeline` é vivo (`EditorApp.ts:650`, `AnimationEditorPanel.ts:352-353` re-parenta `timeline.container`, `:718-780` usa `setDuration/addTracks`); `Component` (`ecs/Component.ts:3-5`) não tem `dispose`; `AnimationStateMachine.serialize()/deserialize()` existem (`:359-381`); `EditorTabRegistry.ts:31-40` registra **10** abas; a aba Scene tem 5 `<canvas>` além do WebGL (`EditorViewport.ts:199/227/232/615`, `SceneGizmos.ts:216`); não existe `__bf`/`__BF__` (só `(editorApp as any)._currentProjectId`, `main.ts:68`); `removeEventListener` = **72**; templates devolvem closures `updateFn` com `resetState` em `ProjectTemplates.ts:1127/1532/3534`; `EventBus.off` é real (`:38`); `generateCollisionMesh` tem dois chamadores (`ProjectTemplates.ts:209`, `TerrainEditorPanel.ts:977`); `NetworkManager` chama `tryReconnect` em `onclose` (`:96`) **e** em `update()` (`:187`) após `ws.close()` — reconnect duplo; `PrefabManager.ts` (318 linhas) e `SaveManager.ts` (222) são construídos em `Engine.ts:219/224` e nenhum outro arquivo os usa; `fengari`/`fengari-interop`/`fflate` estão no lockfile apenas como transitivos (de `fengari-web` e `three`) — viram dependências diretas quando `fengari-web` sair. Codificação: 11 arquivos corrompidos (`LuaScriptRunner.ts` BOM + 496 chars GBK; `EditorMenuBar.ts` 4; `ModelingRiggingPanel.ts` 27; `ElectronExporter.ts` inválido + 14; `PluginSystem.ts` inválido + 1; `docs/API.md` 56; `docs/GETTING_STARTED.md` inválido + 6; `docs/tutorials/01..04` inválidos + 53) e 2 a inspecionar manualmente (`TerrainEditorPanel.ts`, `EngineSystemsPanel.ts` têm 3 caracteres fora do ASCII na faixa CJK que podem ser legítimos).

---

## 1. Arquitetura alvo

### Layout (fim da F9)

```
client/src/
  core/        Engine (fachada ~200 linhas), loop/GameLoop, loop/ModuleRegistry, EventBus, InputManager,
               AssetManager (+assets/assignTexture, assets/loaders/*), SceneManager, render/PostFx
  ecs/         World, Entity, Component (static typeId, dispose?), ComponentRegistry, CollisionEvents,
               System (phase 'fixed'|'update'), components/*, systems/*
  modules/     physics/ (PhysicsBackend, RapierPhysics), audio/, particles/, environment/ (Sky+Weather, dono do fog),
               animation/, navigation/, gameplay/ (starter kit + Dialogue/Quest/…), ui/ (UIManager + Lobby),
               render-extras/ (LOD, Instanced, Decal, Trail, Water, Terrain), network/  — cada um é EngineModule
  scripting/   ScriptHost, LuaHost (fengari), ScriptAPI (api/{scene,input,physics,audio,timer,event,tween,ui,animation,network,debug}),
               ScriptSystem, templates.ts, blueprint/{types,NodeLibrary,nodes/*,GraphRuntime}, BlueprintSystem
  project/     ProjectFile v2, SceneSerializer (único), ProjectStorage + Idb/Fsa, AssetDatabase (GUID), AssetImporter,
               formats/{MaterialFile,UILayout,Clip,Sequence,Terrain}, migrations/, zip (fflate), templates/*.json
  runtime/     GameRuntime (headless-capable; bootGame) — usado pelo Play, pelos testes e pelo export
  export/      PackExporter, ExportManifest, ElectronExporter, templates/*
  editor/      EditorApp (composition root, dispose real), core/{EditorPanel,PanelHost,CommandRegistry,ShortcutService,
               ViewportSurface,ViewportModeController,EditorBus,EditorStore}, ui/{dom,html,tokens,dialogs,ContextMenu,GraphCanvas,Timeline},
               panels/*, modes/{Viewport3DMode,Viewport2DMode,MeshEditMode,RigMode,TerrainMode}, commands/*, serviços já extraídos
  workers/     erosionWorker
server/src/    server.ts (createGameServer), rooms/, index.ts     shared/  types.ts, protocol.ts (type guards)
scripts/       metrics.mjs, check-encoding.mjs, check-readme-claims.mjs, copy-decoders.mjs, make-fixtures.mjs, export-smoke.mjs
tests/ (unit, arch, bench, fixtures)   e2e/ (Playwright)
```

### Loop único, modos e ciclo de vida

`core/loop/GameLoop.ts`: `tick(now)` → `dt = min(now − last, 0.25)`; acumulador `FIXED_DT = 1/60`, máx. 5 substeps, `alpha = acc/FIXED_DT`. Por frame: N × (`physics.step()` + `world.fixedUpdate()`), depois `world.update(dt, alpha)`, `modules.update(dt)`, `frameHooks`, `render(activeCamera)`, `input.endFrame()`. O `Engine` é o único dono do `requestAnimationFrame`; expõe `tick(dt)`/`render(camera)` síncronos (testáveis em node com `EngineConfig.rendererFactory`). **`engine.setMode('edit'|'play')` nasce na F2** e substitui os 20 usos de `editorActive` + `physics.enabled` espalhados (`EditorPlayModeCoordinator.ts:63-178`, `main.ts:71-73`): em `edit` só `TransformSyncSystem` roda. Um `THREE.WebGLRenderer` em `engine.renderer`; painéis 3D do editor usam `ViewportSurface` (renderer com `dispose()` + `forceContextLoss()`), e a partir da F7 os editores de Modeling/Rig/Terrain/Animation/Cinematic são **modos** do viewport principal (`ViewportModeController`, nascido na F5). Módulos implementam `EngineModule { id; init(ctx); fixedUpdate?; update?; dispose() }` e entram por `engine.use()`.

**`GameRuntime` nasce na F4**, não na F8: é `core + modules + scripting + project` compostos sem editor, com `step(n)` headless e `rendererFactory` nulo. O Play do editor, `tests/samples.test.ts` (F6) e o export (F8) usam a mesma classe.

### Peça → base atual → o que morre

| Peça | Vira base | Morre |
|---|---|---|
| `core/Engine` | `engine/Engine.ts` (renderer, câmera, clock, dispose) | 60 campos públicos ansiosos (17 `new *System()` no construtor), `optimizeFrame/restoreAfterRender` (`:468-508`), `initWebGPU` (`:291`), `NetworkManager` no construtor (`:202`), culler, `PerformanceManager` (preset nunca aplicado, `:237`), `PrefabManager`, `SaveManager` |
| ECS | `ecs/World.ts` (índice invertido), `Entity`, `System`, `PhysicsSystem`, `CharacterControllerSystem`, `AnimationSystem` (já faz `bind`, `:23-32`) | `tickBudgetMs`, pools, `systemFrameMs.clear` (`:224`), chave por `type.name`+`includes` (`:56,:261`), Euler duplicado (`GameComponents.ts:8-21`), `NetworkComponent` no núcleo |
| Física | `engine/RapierPhysics.ts` (corpos, KCC, raycast, joints) | `timestep = min(dt,1/30)` (`:523`), filtro `>= 0` (`:531`), `TerrainSystem.generateCollisionMesh` (`:818`) e seus 2 chamadores, `workers/PhysicsWorker.ts` |
| Assets | `engine/AssetManager.ts` (`extractModelData :174` já devolve `animations :211`, `loadModelFromFiles :255`, KTX2), `MaterialLibrary`, `MemoryBudget` (estatística) | CDN (`:60,:581`), sRGB incondicional (`:101,:121`), `scene.clone(true)` (`:220`), `streamQueue`, `AssetPipeline`, `AssetDependencyGraph` |
| Scripting | forma da API de `LuaScriptRunner`, `LUA_API_REFERENCE :15`, `EXTENDED_LUA_TEMPLATES :296`, `trackToLua :952`, 50 scripts Lua dos templates, backends reais (`EventBus/Timer/TweenManager/AudioManager/InputManager`) | `ScriptingSystem.ts`, transpiler regex + `new Function` (`:725`), `LuaDebugger/LuaHotReloadManager`, `fengari-web`, `userData.__luaScript/__blueprint`, textarea de script do inspector (`EditorInspector.ts:698-716`) |
| Blueprint | `VisualScriptEditor.ts` tipos (19-105), `NODE_LIBRARY` (107-1548), `GraphRuntime` (1574-1825), UI (1826+) | `connections.find` (`:1656,:1734`), `setTimeout` (`:1706`), breakpoint só `_paused` (`:1688`), 79 nós sem `execute`, `pushUndo` próprio (`:1914`), keydown global (`:2194`) |
| UI/HUD | `UIManager.ts` (`registerLayout/loadLayout :315-321`, `UILayoutData :527`), `UIEditorPanel.ts` (`UILayoutDef :73`, 5 templates) | `localStorage['blindfake_ui_layouts']` (`UIEditorPanel.ts:1188/1194`), HUD DOM imperativo dos templates (`ProjectTemplates.ts:389…3867`) |
| Projeto/serializer | `SceneSerialization.ts` (+teste), `EditorProjectFileService`, `EditorAutosaveService`, `EditorPlayModeCoordinator` (+testes) | `BuildExportSystem.SceneSerializer`, `blindfake_autosave` global, `LandingPage.sceneData`, `exportToFile/importFromFile` (`EditorApp.ts:1186`, `EditorMenuBar.ts:231/277`), `PlayModeSystem` snapshot (F4) |
| Runtime/export | `GameRuntime` (F4); geradores `generatePackageJson/MainJs/PreloadJs/Readme` de `ElectronExporter.ts` (já emitem `contextIsolation/sandbox`) | `GameExporter.ts`, `BuildExportSystem.ts`, `ElectronExporter.ts` (CSP `:253`, viewer `:269`) |
| Rede | `NetworkManager.ts`, `server/src/index.ts`, `shared/types.ts` (payloads corretos em `:124/:130`) | `ClientPrediction`, `EntityReplicator`, `RoomManager`, `payload: any` (`:53`) |
| Editor | `EditorApp` + serviços testados, `EditorTimeline` (vivo — sai só na F7), `TextureEditorPanel` (paint real, fica como Parcial), 32 tokens `--ed-*` | `EditorManager` e órfãos, `AssetBrowser`, `MaterialEditor`, `MultiSelect`, `KeybindEditor` (volta como `ShortcutsPanel` na F5), seção Plugins, renderers por aba (F7), 224 `innerHTML =`, 567 `cssText =`, 32 diálogos nativos, 5 `escapeHtml`, 13 injeções de `<style>` |

---

## 2. Fases

### F0 — Baseline, gates, drift de configuração e smoke e2e (4-6 dias)

**Objetivo.** Instalar, medir o estado real, colocar CI/lint/typecheck (incluindo `tests/`) e escrever o **primeiro** Playwright smoke antes de qualquer corte.
**Por que agora.** Sem baseline reproduzível e sem smoke não dá para provar que a F1 não quebra o editor; `tests/` nunca foi type-checado; `build:server` emite em `dist/server/server/src`.
**Entregáveis.** `npm run check` verde local e no CI (Node 20.19 e 22); `docs/METRICS.md` com baseline corrigido; tag `pre-rework` em `caa28c7`; `scripts/metrics.mjs`, `scripts/check-encoding.mjs`; `e2e/editor.smoke.spec.ts` v0; `knip.json`.

| Caminho | Ação | Nota |
|---|---|---|
| `package.json` / `package-lock.json` | modify | scripts `typecheck`, `typecheck:test`, `typecheck:server`, `lint`, `test:coverage`, `test:e2e`, `start`, `check`, `metrics`; `engines.node >=20.19`; `@types/express@^4`; devDeps `eslint@9`, `typescript-eslint`, `eslint-plugin-no-unsanitized`, `@vitest/coverage-v8`, `@playwright/test`, `knip`; travar `three`/`rapier` em versões exatas; regenerar lockfile |
| `tsconfig.json`, `tsconfig.test.json` (novo), `tsconfig.server.json` | modify/create | remover `declaration/declarationMap` e alias `@game/*`; `tsconfig.test.json` inclui `tests/**`; server `outDir: dist` com `rootDir: .` |
| `vite.config.ts`, `vitest.config.ts`, `playwright.config.ts` (novo) | modify/create | remover `@game`; coverage v8; Playwright com `webServer: vite`, Chromium headless com `--use-gl=angle --use-angle=swiftshader` |
| `eslint.config.js`, `.editorconfig`, `.nvmrc` (22.12), `.gitignore`, `knip.json` | create/modify | `no-new-func`/`no-implied-eval` **error** (2 ocorrências, `eslint-disable` com ticket até F4); `no-unsanitized/property|method`, `alert|prompt|confirm` e strings `cdn.jsdelivr.net|gstatic.com|unpkg.com` como **warn**; knip com `entry: ['client/src/main.ts','client/src/engine/workers/*.ts','server/src/index.ts']`, plugin vite, `ignore: ['client/src/network/NetworkManager.ts', 'client/src/ecs/components/NetworkComponent.ts']` (ticket F9) |
| `.github/workflows/ci.yml` | create | matrix 20.19/22; `typecheck` ×3, `lint`, `test:coverage`, `build`, `build:server`, `check-encoding` (continue-on-error até F2), `knip` (continue-on-error até F1), `test:e2e`, artefato `metrics.json` |
| `scripts/check-encoding.mjs` | create | `TextDecoder('utf-8',{fatal:true})` em `.ts/.md/.css/.html/.json`; falha em BOM, U+FFFD e qualquer char U+3000–U+9FFF (allowlist vazia; os 3 chars de `TerrainEditorPanel.ts`/`EngineSystemsPanel.ts` são revisados e trocados ou allowlisted por linha) |
| `scripts/metrics.mjs` | create | conta separadamente **produto** (client+server+shared) e **tests**; `innerHTML =`, `cssText =`, `alert|prompt|confirm(`, `addEventListener`/`removeEventListener`, `new THREE.WebGLRenderer`, CDN, `new Function`, `constructor.name`, `this.editor.`, `<style>`, módulos sem importador (via knip JSON), `it(`, asserções fracas |
| `e2e/editor.smoke.spec.ts` v0 | create | `addInitScript` embrulha `HTMLCanvasElement.prototype.getContext` e conta contextos `webgl/webgl2` criados e eventos `webglcontextlost`; para cada um dos 3 templates que sobrevivem à F1 (`3d`, `2d`, `platformer3d`): abrir, zero `pageerror`, abrir as 10 abas e voltar para Scene, Add→Cube, Ctrl+Z, F9 play/stop. Contextos WebGL ≤ 8 (baseline), decrescendo nas fases |
| `tests/*.test.ts` | modify | corrigir erros que `typecheck:test` revelar; testes que morrem na F1 recebem `// TODO(F1)` |

**Cortes.** Nenhum arquivo de produto; só `@game/*` e `declaration*`.
**Reuso.** Configs existentes, os 36 testes, `shared/types.ts`.
**Verificação.** `nvm use && npm ci && npm run check` exit 0; `npm run build:server && ls dist/server/src/index.js dist/shared/types.js`; `npm start` responde em `/api/health`; `node scripts/metrics.mjs` reproduz o baseline da seção 5 (inclusive **72** `removeEventListener`); `npx playwright test editor.smoke` verde nos 9 templates atuais (o teste itera `ProjectTemplate` e é reduzido na F1); `git tag -l pre-rework`.
**Riscos.** Volume de erros em `tests/` (timebox 1 dia); SwiftShader lento no CI (timeouts de 60 s por template).
**Esforço.** 4-6 dias.

### F1 — Corte agressivo (5-7 dias)

**Objetivo.** Remover ≈23k linhas inalcançáveis ou de fachada em 5 PRs ordenados por importador, mantendo o editor abrindo e as 10 abas funcionando.
**Por que agora.** Cada bug seguinte fica menor; `OcclusionCulling` é apagado em vez de corrigido.
**Entregáveis.** `knip` = 0 no CI (com o `ignore` de rede); README/ROADMAP/GETTING_STARTED honestos e recodificados; `check-readme-claims` no CI; `docs/METRICS.md`: produto ≤ 53k linhas, 0 módulos mortos, ~143 testes reais.

| Caminho | Ação | Nota |
|---|---|---|
| 14 módulos sem importador (`2d/Physics2D`, `2d/Sprite2D`, `ai/*`, `engine/{CameraController,EditorEventBus,LightingManager,PerformanceMonitor,ProceduralGeneration,RaycastUtils,VegetationSystem,AssetManifest}`, `gameplay/InventorySystem`, `workers/PhysicsWorker`) | delete | `Sprite2D.ts` (`SpriteSheet :33`, `SpriteAnimationClip :24`) copiado para `docs/decisions/` antes de apagar; recriado como componente na F6 |
| 7 órfãos do editor (`EditorManager`, `CinematicEditorPanel`, `ParticleEditorPanel`, `ModelInspectorPanel`, `LayoutPresets`, `PanelPersistenceService`, `SceneVersioning`) + `KeybindEditor` (só tipo em `EditorKeyboardShortcuts.ts:22`; remover `useKeybindEditor :106`) | delete | rebind volta na F5 como `ShortcutsPanel` |
| 11 módulos só-teste + 11 testes (`UISystem`, `AudioMixer`+`AudioEffects`, `TouchInputManager`, `VirtualJoystick`, `Benchmark`, `SpatialAudioManager`, `AdaptiveMusicSystem`, `ClientPrediction`, `EntityReplicator`, `RoomManager`; `tests/BuildSystem.test.ts`) | delete | 181 testes que só provavam módulos mortos; `tests/NetworkManager.test.ts` fica (reescrito na F2) |
| Fachadas do Engine: `WebGPURenderer`, `GameExporter`, `ElectronExporter`, `BuildExportSystem`, `PluginSystem`, `HotReload`, `WorkerManager`, `AssetPipeline`, `AssetDependencyGraph`, `Profiler`, `LightProbeSystem`, `OcclusionCulling`, `SplineSystem` | delete | os três exportadores saem juntos (`ElectronExporter.ts:18` importa `BuildExportSystem`); seção Plugins de `EngineSystemsPanel.ts:47,83,1375-1425` sai no mesmo PR |
| `client/src/engine/Engine.ts` | modify | remover imports/campos/dispose dos módulos acima e `NetworkManager` (`:13,:84,:202,:395,:532` — arquivo fica, knip-ignored até F9); `initWebGPU`; bloco do culler |
| `editor/PlayModeSystem.ts`, `panels/EditorMenuBar.ts`, `EditorApp.ts`, `EditorLayoutBuilder.ts`, `EditorTabController.ts` | modify | remover import de `BuildExportSystem` e campo `snapshot` nunca lido; menu sem `GameExporter` (`:3`), 'Export Game (HTML)' (+ label `Ctrl+E`), 'Export Game (ZIP Folder)', os dois 'Export Scene JSON', 'Asset Browser' (`:200` → `switchTab('scene')`), 'Exit to Game' (`:132`) e 'Timeline' (`:197`, só alternava o div vazio); remover `assetBrowser/materialEditor/multiSelect` (`EditorApp.ts:361-365`) e o `timelinePanel` descartado (`EditorLayoutBuilder.ts:69-72`, `EditorTabController.ts:7,17,26,91-92`). **`EditorTimeline` fica** (usado pela aba Animation) |
| `panels/AssetBrowser.ts`, `panels/MaterialEditor.ts`, `MultiSelect.ts` | delete | construídos e nunca renderizados (~2,7k linhas) |
| `editor/TerrainEditorPanel.ts:994` | modify | opção `'river'` do seletor de água removida da UI (`case 'river': break;` é fachada); volta na F7 se couber no `TerrainMode` |
| `ProjectTemplates.ts`, `LandingPage.ts`, `tests/ProjectTemplates.test.ts` | modify | apagar `setupFPS/TopDown2D/MainMenu/Fighting/Racing/Puzzle` e seus `SCRIPT_*` (≈2,7k linhas); ficam `setup3DDefault`, `setup2DEmpty`, `setupPlatformer3D`; `ProjectTemplate` (`LandingPage.ts:6`) encolhe para `'3d'|'2d'|'platformer3d'`; o teste perde 7 casos (`:100,:114,:181,:195,:208,:222,:237`) e mantém `resetState` do platformer (`:155`) |
| `docs/API.md`, `docs/tutorials/` | delete | símbolos inventados + codificação inválida; voltam na F9 via typedoc |
| `README.md`, `ROADMAP.md`, `docs/GETTING_STARTED.md`, `panels/EditorMenuBar.ts`, `scripts/check-readme-claims.mjs` | modify/create | **recodificar** `GETTING_STARTED.md` (UTF-8 inválido + 6 U+FFFD; Node 18+ → 20.19) e os 4 U+FFFD de `EditorMenuBar.ts:127-128`; tabela de status por feature (Funciona/Parcial/Beta/Removido/Planejado); denylist: WebGPU, PWA, Physics Worker, delta compression, interest management, Shader Graph, `website/`, "345 tests" |

**Cortes.** ≈23k linhas (17,9k nos arquivos listados + 2,7k de templates + 2,2k de testes + docs + trechos de `Engine`/menu); README deixa de anunciar 12 features.
**Reuso.** `SceneSerialization.ts`, `PlayModeSystem` (restore por `objectStates`), `EditorSelectionController`, `gameplay/BehaviorTree` (a versão importada em `Engine.ts:59`), `EditorTimeline`.
**Verificação.** Após cada PR `npm run check && npx knip && npx playwright test editor.smoke` (agora com 3 templates e sem seção Plugins); `grep -rn 'GameExporter\|BuildExportSystem\|OcclusionCulling\|PluginSystem\|MultiSelect\|AssetBrowser' client/src` vazio; `check-encoding` passa em `docs/` e `EditorMenuBar.ts`; manual: aba Animation ainda mostra a timeline e "Insert clip into timeline" funciona.
**Riscos.** Perder as 6 demos jogáveis (tag `pre-rework`; 3 voltam como samples na F6); `EditorAssetDropService` não depende do `AssetBrowser` (teste existente).
**Esforço.** 5-7 dias.

### F2 — Bugs de runtime: loop único, modo, autosave, servidor, worker, codificação, física, ECS (8-11 dias)

**Objetivo.** Eliminar defeitos estruturais visíveis com o e2e já como gate; `git mv` da física antes de editar.
**Por que agora.** Pré-requisito da composição (F3) e do contrato de painéis (F5).
**Entregáveis.** Um RAF e um `WebGLRenderer` no editor; `GameLoop` com passo fixo; `engine.setMode`; `Component.dispose`; autosave por projeto; servidor que não cai; reconnect sem duplicar; worker de erosão em produção; 0 mojibake/BOM; contatos com terreno; `e2e/editor.smoke.spec.ts` v1.

| Caminho | Ação | Nota |
|---|---|---|
| `engine/RapierPhysics.ts` → `modules/physics/RapierPhysics.ts`, `ecs/systems/PhysicsSystem.ts` | move (PR só de `git mv`) | preserva blame; edições vêm no PR seguinte |
| `engine/loop/GameLoop.ts` (novo) + `Engine.ts` | create/modify | acumulador 1/60, máx 5 substeps, clamp 0,25 s, alpha; `tick(dt)`, `render(camera)`, `frameHooks`, `activeCamera`, `setViewportSize`, `rendererFactory`; **`setMode('edit'|'play')`** (em `edit` só `TransformSyncSystem`; em `play` física + sistemas); `initRapier().catch` → `events.emit('physics:error')`; remover `optimizeFrame/restoreAfterRender` (`:468-508`) e `logarithmicDepthBuffer` (`:171`) |
| `editor/EditorApp.ts`, `EditorPlayModeCoordinator.ts`, `panels/EditorViewport.ts`, `main.ts` | modify | apagar `editorRenderer/editorCanvas` (`:179-193`) e `loop` (`:685`); corpo vira `onFrame(dt)` em `frameHooks`; `engine.renderer.domElement` re-parentado no viewport; coordinator usa `engine.setMode` (substitui `:63,:67,:94,:104,:175,:178`); `main.ts` sem luz duplicada, sem `_currentProjectId` (`:68`, `projectId` por construtor), sem `editorActive/physics.enabled` (`:71-73`) e **sem `gameContainer.style.display='none'` (`:74`)** — o `#game-container` passa a ser o pai do viewport e `#ui-overlay` fica visível em play |
| `ecs/Component.ts`, `components/GameComponents.ts`, `ecs/World.ts` | modify | **criar** `dispose?(): void` em `Component` e `MeshComponent.dispose()` (`object3D.removeFromParent()`; geometria/material só com refcount da F3); `World` chama `dispose` no flush de `entitiesToRemove`; timings em double buffer; remover `tickBudgetMs/componentPools/acquireComponent`; chave de arquétipo por id numérico |
| `editor/EditorAutosaveService.ts`, `EditorSceneEditingService.ts`, `LandingPage.ts` | modify | chave `blindfake_autosave:<projectId>`; `offerRestore` chama `replaceSceneContent()`; `saveProjectScene` passa a ser chamado |
| `server/src/server.ts` (novo), `server/src/index.ts`, `shared/protocol.ts` (novo) | create/modify | `createGameServer({port})`; `maxPayload: 64KB`; `try/catch`; type guards por tipo (`null`, `ping`/`HANDSHAKE`/`CREATE_ROOM`/`JOIN_ROOM`/`CHAT_MESSAGE` sem payload, `playerName` numérico); `maxPlayers` inteiro 1..8; handshake repetido rejeitado; `ROOM_UPDATE/ROOM_LIST` conforme `RoomUpdatePayload/RoomListPayload`; `/api/dev-errors` só fora de produção |
| `network/NetworkManager.ts`, `tests/NetworkManager.test.ts` | modify/rewrite | URL default de `location` (`/ws`); `connect()` rejeita em erro/timeout; `disconnect()` não zera `maxReconnectAttempts` (`:171`); flag `reconnecting` (hoje `update():187` e `onclose:96` agendam duas reconexões); teste reescrito **sem `as any`** contra servidor `ws` real em porta 0 + fake timers: 5 tentativas com backoff e para; pong timeout gera 1 reconexão, não 2 |
| `editor/TerrainEditorPanel.ts:1075-1077` | modify | `new Worker(new URL('../engine/workers/erosionWorker.ts', import.meta.url), { type: 'module' })` inline |
| `cinematics/LuaScriptRunner.ts` (BOM + 496 chars GBK — revisão manual de comentários/strings, `git show` não ajuda), `panels/ModelingRiggingPanel.ts` (27) | modify | recodificar; `check-encoding` vira blocking |
| `modules/physics/RapierPhysics.ts`, `ecs/systems/PhysicsSystem.ts` | modify | `world.timestep = 1/60` fixo, `step()` sem dt, filtro `eA !== undefined && eB !== undefined` (ids de terreno são negativos, `PhysicsSystem.ts:15`) |
| `panels/VisualScriptEditor.ts:2194`, `panels/EngineSystemsPanel.ts:1546`, `styles/editor.css:43-44`, `engine/DevErrorTracker.ts` | modify | keydown ignorado se `!container.isConnected` e removido no dispose; `DOMNodeRemoved` → `dispose()`; regras `.editor-mode #game-container/#ui-overlay { display:none }` removidas; tracker só em DEV |
| `tests/{GameLoop,ECS}.test.ts`, `tests/server/protocol.test.ts`, `tests/arch/single-renderer.test.ts`, `e2e/editor.smoke.spec.ts` v1 | create/modify | 3 frames de 50 ms → 9 passos, clamp e teto; `setMode('edit')` roda só `TransformSync`; `removeEntity` remove o `Object3D` da cena; servidor real recebendo `null`/`ping`/1 MB/handshake duplo; `new THREE.WebGLRenderer` ≤ 6; smoke: contextos WebGL criados em Scene = 1, `webglcontextlost` = 0, F9 play mostra `#ui-overlay` |

**Cortes.** Segundo renderer/loop; `optimizeFrame`; chave global de autosave; pools/budget; `editorActive`; hack `_currentProjectId`.
**Reuso.** `EditorPlayModeCoordinator` (+teste), `EditorMenuBar.loadScene`, `shared/types.ts`.
**Verificação.** `npm run check && npx playwright test`; manual: play no platformer estável; reabrir projeto não duplica; `npm run build && npm run preview` → erosão termina; `node -e` enviando `null` via `ws` não derruba `npm run dev:server`; matar o servidor com o cliente conectado → console mostra 5 tentativas e para.
**Riscos.** Passo fixo muda a sensação do KCC (ajustado na F3); mover o canvas muda `pixelRatio` (`setViewportSize`).
**Esforço.** 8-11 dias.

### F3 — Núcleo confiável: composição, ECS com identidade, física correta, post-fx real, assets sem CDN (2,5-3 semanas)

**Objetivo.** God object → núcleo pequeno + módulos opt-in; `typeId`; heightfield + contatos + interpolação; `EffectComposer`; decoders locais; destino definido para cada subsistema.
**Por que agora.** F4 precisa de API estável e `CollisionEvents`; o serializer v2 precisa de `typeId`.
**Entregáveis.** `core/` + `modules/*` com `EngineModule`; `createDefaultEngine()`; `ComponentRegistry`; `PhysicsBackend`; `CollisionEvents`; `PostFxModule`; `copy-decoders.mjs`; `assignTexture`; thresholds de cobertura; benches de física, cloth, navmesh e IK; ADR do worker.

| Caminho | Ação | Nota |
|---|---|---|
| `core/{Engine,ModuleRegistry,EventBus,InputManager,AssetManager,SceneManager}.ts` | move/modify | `git mv` por pasta; aliases `@core/@modules/@ecs/@editor/@shared`; construtor sem `new *System()`; `use(module)`, `module<T>()`; getters de compatibilidade até F5 |
| `engine/PerformanceManager.ts`, `PrefabManager.ts`, `SaveManager.ts` | delete | `setQuality` nunca aplica o preset inicial (`:237` vs `:127`) e nada consome; `PrefabManager`/`SaveManager` sem consumidor (prefabs voltam como formato de dados na F6 se o dono quiser — pergunta 12) |
| `RenderStatsOverlay` → `editor/panels/StatsOverlay.ts`; `LODSystem`, `InstancedRenderer`, `DecalSystem`, `TrailRenderer`, `WaterSystem`, `TerrainSystem` → `modules/render-extras/`; `MemoryBudget` → `core/AssetManager` (estatística) | move | `EngineSystemsPanel.ts:623` e `:1015-1023` passam a ler via `engine.module(LODModule)` |
| `ecs/Component.ts`, `ecs/ComponentRegistry.ts` (novo), `ecs/System.ts`, `ecs/World.ts`, `ecs/CollisionEvents.ts` (novo) | modify/create | `static readonly typeId`; `register(typeId, ctor, {serialize, deserialize})`; `System.phase`; `World.fixedUpdate/update(dt, alpha)`; `getEntityByObject` via `userData.entityId`; buffer `{a,b,started,aIsStatic,bIsStatic}`; lint proíbe `constructor.name` (7 usos: `EditorInspector.ts:793/830/909`, `EngineSystemsPanel.ts:1466`, `EditorViewport.ts:736`, `AnimationEditorPanel.ts:1488`, `gameplay/BehaviorTree.ts:39`) |
| `components/GameComponents.ts` → `components/{Transform,MeshRef,PhysicsBody,Animation,StateMachine,CameraFollow,Health}.ts`; `GameComponents2D.ts` → `GameplayComponents.ts` | split/move | `TransformComponent` sem Euler (+`setYaw()`); `CharacterControllerSystem.ts:155-161`, `AISystem.ts:56-57`, `RenderSystem.ts:36` migram; `PhysicsBody` ganha `prevPosition/prevRotation`; `NetworkComponent` → `network/` (knip-ignored) |
| `modules/physics/{PhysicsBackend,RapierPhysics}.ts`, `ecs/systems/PhysicsSystem.ts`, `RenderSystem.ts` → `TransformSyncSystem.ts`, **`ProjectTemplates.ts:209`, `TerrainEditorPanel.ts:977`** | create/move/modify | `addHeightfieldBody(nrows, ncols, heights, scale)`: Rapier recebe **subdivisões** e `heights` com `(nrows+1)×(ncols+1)` valores **column-major**; para o template (`resolution: 128`) → `nrows = ncols = 127`; ambos os chamadores de `generateCollisionMesh` (`:818`, morre) trocados nesta fase; interpolação por alpha no `TransformSync` |
| `modules/postfx/PostFxModule.ts` (novo); `engine/PostProcessing.ts` | create/delete | `EffectComposer` + `RenderPass/UnrealBloomPass/GTAOPass/SMAAPass/OutputPass`; 1.687 linhas morrem |
| `modules/environment/EnvironmentModule.ts` (funde `SkySystem`+`WeatherSystem`) | merge | dono único de `scene.fog` (`WeatherSystem.ts:453-457`); **PMREM gerado de uma cena privada só com o domo** (`PMREMGenerator.fromScene(skyScene)`) e só quando parâmetros do céu mudam — hoje `generatePMREM` (`SkySystem.ts:287`, chamado em `:158`) captura a cena inteira; `FogSystem` morre |
| `modules/{audio,particles,animation,navigation,gameplay,ui}/` | move | cada um embrulhado num `EngineModule`; `gameplay/` recebe os 8 sistemas que só `Engine.ts` importa |
| `modules/navigation/NavMeshSystem.ts`, `modules/animation/IKSystem.ts`, `modules/render-extras/ClothSimulation.ts`, `ecs/systems/AISystem.ts` | modify (mínimo) + backlog | `AISystem.ts:71` reutiliza um `Set` por trigger (fix nesta fase); `NavMesh.getPolyById :300` vira `Map` e `buildAdjacency :202` usa hash de arestas; Cloth/IK ganham benches com orçamento e ficam "Beta" no README até zerarem alocações no loop (backlog com gate, ver Verificação) |
| `core/AssetManager.ts`, `core/assets/assignTexture.ts`, `scripts/copy-decoders.mjs` | modify/create | `postinstall` copia `draco/gltf` e `basis` → `assets/public/decoders/`; `map/emissiveMap → SRGBColorSpace`, `normal/roughness/metalness/ao → NoColorSpace`; `cloneModel` via `SkeletonUtils.clone`; `streamQueue` removida |
| `panels/EditorInspector.ts`, `panels/EngineSystemsPanel.ts`, `ProjectTemplates.ts` | modify | inspector por `ComponentRegistry.list()`; painel itera `engine.modules`; templates usam `createDefaultEngine` |
| `tests/{ModuleRegistry,Engine.loop,RapierPhysics,PhysicsSystem,PostFxModule,AssetManager,EnvironmentModule}.test.ts`, `tests/bench/{physics,cloth,navmesh,ik}.bench.ts`, `docs/decisions/0001-physics-worker.md` | create | Rapier real em node: cubo cai e para em y≈0,5 em 120 passos, duas execuções iguais (`toBeCloseTo(…,10)`); contato dinâmico×estático com `bIsStatic=true`; heightfield 128×128 com pico assimétrico em (x=10,z=100) acerta raycast (pega column-major e o off-by-one 128/127); `PostFxModule` com `rendererFactory` fake asserta passes registrados na ordem, `setSize` propagado e `dispose` chamado em cada pass; `EnvironmentModule` gera PMREM 1× após 100 updates sem mudança de parâmetro; benches: 2000 corpos, cloth 32×32 × 60 passos, navmesh 5k polys A*, IK FABRIK 3 ossos × 10 iterações — orçamento registrado em `metrics.json` e regressão > 20% falha |

**Cortes.** Construção ansiosa de ~50 subsistemas; `PostProcessing.ts`; `PerformanceManager`; `PrefabManager`; `SaveManager`; CDN; Euler duplicado; `constructor.name`; trimesh de terreno; `FogSystem`.
**Reuso.** `World.ts`, `RapierPhysics.ts`, `AudioManager/ParticleSystem/TweenManager/Timer/EventBus` (+testes), `EngineSystemsPanel` como inspetor genérico.
**Verificação.** `npm run check` com thresholds (core ≥ 70, ecs ≥ 80, modules/physics ≥ 60, server ≥ 70); e2e inalterado; `grep -rn 'new .*System()' client/src/core/Engine.ts` = 0; `git grep -n 'constructor.name' client/src` = 0; `metrics.mjs` conta `new THREE.(Vector3|Quaternion|Matrix4)` dentro de `update/solve/step/satisfyConstraints` de Cloth/IK (baseline 20 e 43) — o número só pode cair; manual: Bloom liga/desliga; `.glb` DRACO offline; `normalMap.colorSpace === ''`; personagem sobe a montanha e `physics:collision` chega ao pousar; sem fog "piscando" ao trocar clima.
**Riscos.** Maior movimentação de imports (um PR por pasta); `GTAOPass` exige depth texture — fallback bloom+SMAA documentado.
**Esforço.** 2,5-3 semanas.

### F4 — Persistência real, scripting Lua com sandbox, blueprints, HUD no projeto, Play = GameRuntime, Starter Kit (4-5 semanas)

**Objetivo.** Formato de projeto versionado com round-trip completo (cenas, entidades, scripts, blueprints, **layouts de UI**); scripts executando em play com API ligada aos subsistemas reais e orçamento de instruções; blueprints pelo mesmo `ScriptAPI`; **`GameRuntime` headless e o Play do editor instanciando-o**; biblioteca oficial de componentes/scripts (Starter Kit); Lua fake e Monaco por CDN removidos.
**Por que agora.** Depende do núcleo composto; primeira entrega de valor direto; destrava samples (F6) e export (F8) sem retrabalho de snapshot.
**Decisão de linguagem.** `ScriptHost` é interface; implementação inicial **`LuaHost` sobre `fengari` + `fengari-interop`** (viram dependências diretas; `fengari-web` sai): contrato já é Lua (inspector, 50 scripts, README); sandbox real — `load(chunk, name, 't', env)` com allowlist e `lua_sethook` `LUA_MASKCOUNT` por coroutine abortando `while true do end` com `ScriptBudgetError`; puro JS, testável em node. Custo ~10-30× vs Lua nativo — adequado a gameplay (bench gate abaixo). `JsModuleHost` é pergunta aberta.
**Decisão de templates.** JSON não carrega closures, logo os player controllers **deixam de ser `updateFn`**: viram componentes registrados (`CharacterControllerComponent` já existe em `GameplayComponents.ts:166` + `CharacterControllerSystem`; `CameraFollowComponent` novo, absorve a sincronização de câmera de `EditorApp.ts:696-701`). Isso resolve a contradição e elimina a antiga pergunta 2.

| Caminho | Ação | Nota |
|---|---|---|
| `project/{ProjectFile,SceneSerializer,ProjectStorage,IdbProjectStorage,zip,migrations}.ts`, `project/formats/UILayout.ts` | create/move | v2 `{ $type:'bf.project', $version:2, meta, settings:{fixedDt,gravity,shadows,toneMapping,mode:'3d'|'2d'}, scenes, scripts, blueprints, **ui: UILayoutDef[]**, animations, assets }`; `SerializedObject.entity?: {id,name,tags,components:[{typeId,data}]}`; campo reservado `SerializedObject.prefab?: {assetId, overrides}` e `$type:'bf.prefab'` (sem implementação; evita migração se a pergunta 12 for "sim"); `ProjectStorage` (IndexedDB agora, FSA na F6); `.bfproject` = zip (fflate); migração v1 (`EditorProjectFileData`, `LandingPage.sceneData`, `userData.__luaScript/__blueprint`, `localStorage['blindfake_ui_layouts']` → `ui`); validação por type guards |
| `runtime/{GameRuntime,index}.ts`, `editor/PlayController.ts` (substitui `PlayModeSystem` + parte do coordinator) | create | `GameRuntime.fromProject(projectFile, {renderer?, canvas?})`, `step(n)`, `start/stop/pause/timeScale`; Play serializa o projeto e cria um `GameRuntime` com `World` + `THREE.Scene` novos renderizados no mesmo canvas; Stop descarta o runtime — snapshot/restore morre já aqui; `UIManager.registerLayout` alimentado por `ProjectFile.ui` no boot do runtime |
| `scripting/{ScriptHost,LuaHost,ScriptSystem,templates}.ts`, `scripting/api/{scene,input,physics,audio,timer,event,tween,ui,debug}.ts`, `ecs/components/{Script,Blueprint}.ts` | create | API ligada a `InputManager`, `PhysicsBackend` por `entityId`, `AudioManager`, `TimerManager`, `EventBus` (**`event.off` real fica em `scripting/api/event.ts` — o no-op era da camada Lua, `EventBus.off :38` sempre existiu**), `TweenManager`, `ui.show/hide/setText/setVar` sobre `UIManager`; `ScriptSystem` fase fixed → `onFixedUpdate`, update → `onUpdate`, `onCollision` de `CollisionEvents`; erro em um script → `script:error` |
| **Starter Kit** — `modules/gameplay/starter/{CharacterController,CameraFollow,Health,Trigger,Pickup,Door,Checkpoint,Lift,Spawner}.ts`, `scripting/templates.ts` (Rotate, Bob, Patrol, Collectible, PressurePlate), `docs/STARTER_KIT.md` | create | registrados no `ComponentRegistry`, listados no "Add Component" do inspector com campos editáveis, documentados com exemplo Lua; player controllers dos 3 templates portados para cá |
| `scripting/blueprint/{types,NodeLibrary,nodes/*,GraphRuntime}.ts`, `ecs/systems/BlueprintSystem.ts`; `panels/VisualScriptEditor.ts` | split/create | extrair 19-105, 107-1548, 1574-1825; `GraphRuntime` com índices por nó, pilha explícita (breakpoint pausa; `stepOnce`), `flow_delay` via `TimerManager`, limite em `flow_for/flow_whileLoop`; 100% dos nós com `execute` ou apagados; o arquivo fica só com a UI |
| `panels/ScriptEditorPanel.ts`, `panels/EditorInspector.ts:616-720` | modify | `monaco-editor` via npm (`?worker`, `import()` ao abrir a aba; remove CDN `:240/:243` e fallback `:250`); imports de `ScriptingSystem`/`LuaScriptRunner` (`:13-18`) somem; **a textarea do inspector (`:698-716`) morre** — "Edit Script" abre a aba Scripting com o `ScriptComponent` selecionado (um único editor de código); inspector escreve `ScriptComponent/BlueprintComponent` |
| `engine/ScriptingSystem.ts`, `cinematics/LuaScriptRunner.ts`, `types/fengari-web.d.ts` | delete/modify | sobrevivem `LUA_API_REFERENCE`, `EXTENDED_LUA_TEMPLATES`, `trackToLua`; `CinematicEngine` (`:3,:54,:85`) compila `intro.lua` no `LuaHost` com builtin `keyframe()` |
| `editor/UIEditorPanel.ts`, `modules/ui/UIManager.ts` | modify | `saveLayout` grava em `ProjectFile.ui` via `EditorStore` (não em localStorage); `UILayoutDef` e `UILayoutData` unificados em `project/formats/UILayout.ts`; HUD dos templates (DOM imperativo em `ProjectTemplates.ts:389…1125`) vira layout `hud` no projeto |
| `ProjectTemplates.ts` → `project/templates/{empty3d,empty2d,platformer3d}.json` | replace | dados puros gerados uma vez; decorativos/trigger por Lua; controllers por componentes do Starter Kit |
| `editor/EditorProjectFileService.ts`, `EditorAutosaveService.ts`, `LandingPage.ts`, `main.ts` | modify | autosave grava o `ProjectFile` inteiro por projeto; landing lista `ProjectStorage.list()`; Ctrl+S salva o projeto |
| `tests/{LuaHost,ScriptApi,ScriptSystem,GraphRuntime,SceneSerializer.roundtrip,ProjectStorage,templates,GameRuntime,PlayController,StarterKit,UILayout}.test.ts`, `tests/bench/scripts.bench.ts`, `e2e/scripting.play.spec.ts` | create | `while true do end` aborta em < 200 ms; `os/io/load` são `nil`; globals isolados; `input.isKeyDown` reflete keydown jsdom; `physics.applyImpulse` chega ao backend stub; grafo `event_start → set_position` move; round-trip deep-equal com Transform/PhysicsBody/Script/Blueprint/UI; `GameRuntime.step(300)` duas vezes → posições iguais (`toBeCloseTo(…,10)`); cada componente do Starter Kit está no registry e em `STARTER_KIT.md`; **bench: 200 scripts `onUpdate` a 60 fps < 4 ms** (falha acima); e2e: cubo com `self.rotation.y = self.rotation.y + dt` gira em play, para no stop, e um layout `hud` com um texto aparece em play |

**Cortes.** `ScriptingSystem.ts`, transpiler + `new Function`, `LuaDebugger/LuaHotReloadManager`, `fengari-web`, `userData.__luaScript/__blueprint`, 79 nós fantasmas, Monaco por CDN, `blindfake_ui_layouts`/`blindfake_projects`/`sceneData` em localStorage, `PlayModeSystem` e snapshot, textarea do inspector, `engine.onUpdate` como mecanismo de gameplay, `ProjectTemplates.ts` imperativo.
**Reuso.** `SceneSerialization.ts` (+teste), `EditorProjectFileService` (+teste), `EditorAutosaveService` (+teste), `EditorPlayModeCoordinator` (+teste), `GraphRuntime`/75 nós com `execute`, forma das APIs de `LuaScriptRunner`, `UIManager.loadLayout`, 5 templates de layout do `UIEditorPanel`.
**Verificação.** `npm run check` (scripting ≥ 80%, project ≥ 85%, runtime ≥ 70%); `git grep -n 'new Function\|fengari-web\|__luaScript\|jsdelivr\|blindfake_ui_layouts' client/src package.json` = 0 (regra de CDN vira **error**); manual: Add Component → CharacterController + CameraFollow no cubo → Play anda com WASD; Add Script (Rotate) → Play gira, Stop volta; loop infinito mostra `ScriptBudgetError` e o editor responde; blueprint anexado roda sem a aba aberta; breakpoint pausa e Step avança; fechar e reabrir restaura projeto, scripts, blueprint e HUD; `.bfproject` reproduz a cena.
**Riscos.** Custo de `fengari-interop` (expor `self.position` como tabela sincronizada no início/fim do hook); reentrância Lua↔JS (callbacks enfileirados pelo `ScriptSystem`); portar 3 templates para dados + Starter Kit (~2 dias cada); F4 é a maior fase — dividir em 4 PRs (project, scripting, runtime/play, starter kit).
**Esforço.** 4-5 semanas.

### F5 — Editor sustentável: contrato de painel, comandos, modos, UI sem strings, undo único, dispose real (3-3,5 semanas)

**Objetivo.** Ciclo de vida de painel e do composition root, `CommandRegistry` alimentando menus/toolbar/atalhos por escopo com UI de rebind, `ViewportSurface` + `ViewportModeController`, `GraphCanvas` compartilhado, helpers de DOM/tokens/diálogos, uma pilha de undo ligada a todos os painéis.
**Por que agora.** Antes de Project panel, importadores e modos (F6/F7) é preciso contrato; listeners vazados e atalhos globais só se resolvem com escopo.

| Caminho | Ação | Nota |
|---|---|---|
| `editor/core/{EditorPanel,PanelHost,CommandRegistry,ShortcutService,ViewportSurface,ViewportModeController,EditorBus,EditorStore}.ts` | create | `EditorPanel { id; mount; unmount; dispose; onActivate? }` + `PanelHost.listen()/raf()` rastreados; comandos `{id,label,shortcut,when,run}` geram menus (Ctrl+N/O/S/Shift+S/O ganham binding real; Ctrl+E reservado para Export na F8); escopo por aba ativa; `ViewportSurface` = renderer + `ResizeObserver` + `dispose/forceContextLoss`; **`ViewportModeController { register(mode); enter(id); exit() }`** com `Viewport3DMode` (gizmo/seleção atuais) como modo default — F6/F7 só plugam modos |
| `editor/EditorApp.ts`, `main.ts`, `panels/EditorConsole.ts` | modify | composition root com **`dispose()` real** chamado em `beforeunload` e `import.meta.hot?.dispose`; `EditorConsole` registra `error/unhandledrejection` (`:182,:185`) via `PanelHost.listen` e os remove; `this.editor.*` de 335 para ≤ 60 |
| `editor/ui/{dom,html,tokens,dialogs,ContextMenu,GraphCanvas}.ts`, `styles/editor.css` | create/modify | `html` tagged template escapando interpolações; `promptDialog/confirmDialog` em Promise (32 nativos); `tokens.ts` mapeia os 32 `--ed-*`; 13 injeções de `<style>` migram para o CSS; **`GraphCanvas`** (pan/zoom, nós, arestas, seleção, hit-test, minimap) usado pela UI de `VisualScriptEditor` e por `AnimStateMachineEditor` (1.071 linhas, hoje canvas próprio) |
| `panels/ShortcutsPanel.ts` (novo), `panels/EditorPreferences.ts` | create/modify | rebind por comando com detecção de conflito, persistido em `EditorPreferences` (substitui o `KeybindEditor` apagado na F1) |
| `editor/EditorSelectionController.ts`, `panels/EditorToolbar.ts:105` | modify | `pivotMode` passa a ser lido: gizmo anexa a um pivô helper no centro da bbox (`center`) ou na origem do objeto (`origin`) e aplica deltas à seleção |
| Painéis 2D/DOM (`EditorHierarchy`, `EditorInspector`, `EditorViewport`, `EngineSystemsPanel`, `VisualScriptEditor`, `ScriptEditorPanel`, `AnimStateMachineEditor`, `TextureEditorPanel`, `UIEditorPanel`, `EditorTimeline`) | modify | um PR por painel: implementa `EditorPanel`; `innerHTML` → `dom/html`; `cssText` → classes; inspector atualiza campos por `object:transformed` sem rebuild; nomes via `textContent` |
| Painéis 3D (`ModelingRiggingPanel.ts:245`, `CinematicEditorTab.ts:228`, `AnimationEditorPanel.ts:390`, `TerrainEditorPanel.ts:422`) e `SceneGizmos.ts:253` | modify (mínimo) | **só** `dispose()` + `forceContextLoss()` no unmount e RAF via `PanelHost.raf` (a migração real para modos é na F7 — evita migrar ~6k linhas duas vezes); o nav cube de `SceneGizmos` usa `ViewportSurface` |
| `editor/UndoManager.ts`, `editor/commands/{PropertyCommand,GraphCommand,TextureStrokeCommand,UILayoutCommand}.ts` | create/modify | ligação explícita: Scene → `PropertyCommand`; Blueprints e State Machine → `GraphCommand` (o `pushUndo :1914` morre); Texture paint → `TextureStrokeCommand` (snapshot `ImageData` da região); UI Editor → `UILayoutCommand`; keyframes de Animation/Cinematic → `KeyframeCommand` na F7 |
| `eslint.config.js`, `tests/editor/{ListenerTracker,panels.lifecycle,EditorApp.lifecycle,EditorBus,CommandRegistry,ShortcutService,ViewportSurface,ViewportModeController,GraphCanvas,EditorInspector.updates,commands}.test.ts` | modify/create | `no-unsanitized/property|method` e `alert/prompt/confirm` viram **error** com `overrides` para `editor/ui/{dom,html}.ts`; cada painel e o `EditorApp` inteiro: mount→unmount→dispose deixa 0 listeners de `window/document` e 0 RAF; inspector: 100 `object:transformed` → 0 chamadas a `rebuild()` e 100 updates de campo; Delete no painel Blueprints não apaga objeto da cena; rebind Ctrl+D → Ctrl+Shift+D funciona e conflito é recusado; cada `*Command` faz `execute/undo` idempotentes |

**Cortes.** 5 `escapeHtml`, 12 context menus artesanais, 32 diálogos nativos, pilha de undo do grafo, keydown global, rebuild do inspector por frame, canvas próprio do `AnimStateMachineEditor`, getters de compatibilidade da F3, `pivotMode` fantasma.
**Reuso.** `EditorTabRegistry/TabController` (+testes), `UndoManager`, `EditorKeyboardShortcuts` (+teste), `EditorPreferences`, serviços extraídos, `editor.css`.
**Verificação.** `npm run lint` com as regras em error; `grep -rn 'innerHTML' client/src --include=*.ts | grep -v editor/ui/` vazio; e2e: abrir cada uma das 10 abas 3× seguidas → contextos WebGL criados − `webglcontextlost` ≤ 6 e nenhum `pageerror`; `tests/arch/single-renderer` limite = 6 (todos com dispose); manual: gizmo com inspector aberto sem travar; Ctrl+Z no grafo, na state machine, no paint e no UI Editor desfaz; Window → Shortcuts rebind persiste após reload.
**Riscos.** Migração mecânica longa (224 `innerHTML`, 567 `cssText`) — lint só vira error quando o contador zera; painéis grandes com timebox e rótulo "beta".
**Esforço.** 3-3,5 semanas.

### F6 — Pipeline de conteúdo: painel de projeto, importação (modelos, texturas, áudio, **clips**), materiais, textura, pasta real, samples e modo 2D (4-4,5 semanas)

**Objetivo.** "Project tree tipo Roblox/Unreal": assets por GUID; importação glb/gltf/fbx/obj+mtl, png/jpg/webp/ktx2, mp3/ogg/wav **e extração de AnimationClips como assets**; thumbnails; materiais como assets; edição de textura (paint como Parcial); pasta real via FSA; samples como projetos rodando no `GameRuntime`; projetos 2D.
**Por que agora.** Depende de `ProjectStorage`/`GameRuntime` (F4) e do contrato de painel/modos (F5); pré-requisito de F7 e F8.

| Caminho | Ação | Nota |
|---|---|---|
| `project/{AssetDatabase,AssetImporter,FsaProjectStorage}.ts`, `core/assets/loaders/{Gltf,Fbx,Obj,Texture,Audio}Loader.ts`, `project/formats/Clip.ts` | create | GUID `asset://<guid>`; loaders de `three/addons`; import settings (escala, up-axis, gerar colisores) com Reimport; **`gltf.animations` (já retornado por `extractModelData :211`) e clips FBX viram assets `clip` ligados ao modelo de origem; `AnimationComponent` referencia clips por GUID**; FSA (Chromium) grava `MyGame/{project.json,scenes/,scripts/,assets/}`; IndexedDB continua o fallback |
| `panels/ProjectPanel.ts` (novo), `EditorAssetDropService.ts`, `EditorLayoutBuilder.ts` | create/modify | árvore Scenes/Scripts/Blueprints/Models/Clips/Textures/Materials/Audio/UI; DnD do SO e para viewport/inspector; painel inferior Project \| Console |
| `panels/EditorInspector.ts` (MaterialSection), `project/formats/MaterialFile.ts`, `project/SceneSerializer.ts` | modify/create | `.material.json` com slots ligados a assets e presets de `MaterialLibrary`; cena referencia `assetId + node`; `mapSrc`/base64 (`SceneSerialization.ts:57,:235`) morrem |
| `panels/TextureEditorPanel.ts` | modify | vira editor de **asset de textura**: canais, ajustes, resize/export e **paint (mantido, `:9,49-53`) gravando no asset** com `TextureStrokeCommand`; rótulo "Parcial" (sem camadas/UV unwrap) — pergunta 11 decide se evolui ou congela |
| `editor/modes/Viewport2DMode.ts`, `ecs/components/Sprite.ts`, `modules/physics/RapierPhysics.ts` | create/modify | plugado no `ViewportModeController` da F5: `OrthographicCamera`, pan/zoom, grid XY, gizmo XY+rotZ; `SpriteComponent` com spritesheet (a partir de `SpriteSheet/SpriteAnimationClip` guardados em `docs/decisions/`); física planar com `RigidBodyDesc.enabledTranslations(true,true,false).enabledRotations(false,false,true)` (`lockTranslations()` travaria todos os eixos) |
| `samples/{platformer-3d,topdown-2d,fps}/`, `LandingPage.ts`, `tests/samples.test.ts` | create/modify | gameplay dos templates apagados na F1 reescrito com Starter Kit + Lua; teste headless carrega cada sample no `GameRuntime` (F4) por 60 ticks sem `script:error` |
| `tests/fixtures/`, `scripts/make-fixtures.mjs` | create | `rigged-cube.glb` (gerado **no teste** com `GLTFExporter`: cubo com 2 ossos + 1 clip); `cube-draco.glb` (gerado por `make-fixtures.mjs` com `DRACOExporter`, commitado, < 50 KB); `cube.fbx` (CC0, < 20 KB, com `LICENSE` ao lado); `cube.obj`+`cube.mtl`+`2x2.png` |
| `tests/{AssetImporter,AssetDatabase,ClipImport,MaterialFile,TextureAsset,bfproject.roundtrip}.test.ts`, `tests/physics/planar.test.ts`, `e2e/project.import.spec.ts` | create | importar `rigged-cube.glb` cria 1 asset `model` + 1 `clip` e `AnimationComponent` toca o clip; `cube-draco.glb` decodifica com `setDecoderPath('/decoders/draco/')` sem rede; `cube.fbx` importa com escala/up-axis das settings; hashes iguais após export/import zip; **planar: corpo 2D com Rapier real cai por 600 passos e `z === 0`, `rotX === rotY === 0`**; e2e: `setInputFiles` de `.glb` aparece em Models e instancia |

**Cortes.** `mapSrc`/base64; `scanSceneAssets`; grafo/"shader editor" do material; templates `TEMPLATES` do painel de textura sem asset.
**Reuso.** `AssetManager.loadModelFromFiles/extractModelData`, `EditorAssetDropService` (+teste), `MaterialLibrary` (+teste), `project/zip.ts`, `editor/ui/*`, `GameplayComponents.SpriteComponent`, `TextureEditorPanel` (paint/canais).
**Verificação.** `npm run check` (project ≥ 85%) e Playwright `project.import`; manual (Chromium): Novo Projeto em pasta → `ls MyGame` mostra `project.json` e `scenes/Main.scene.json`; importar `.fbx` Mixamo real, `.obj+.mtl+.png`, `.glb` DRACO offline; clip importado aparece em Clips e no dropdown do `AnimationComponent`; pintar textura, Ctrl+Z, salvar, reabrir; 2D: sprite de 4 frames com rigidbody cai e anima; Firefox: mesmo fluxo em IndexedDB; README: FBX "Parcial" se as 3 fontes reais (Mixamo, Blender, Maya) não passarem.
**Riscos.** FSA só em Chromium (landing avisa); FBX com unidades/eixos variados; quota de IndexedDB (diálogo próprio).
**Esforço.** 4-4,5 semanas.

### F7 — Animação, modelagem, rig e terreno como modos do viewport (4 semanas)

**Objetivo.** Runtime de animação com state machine **persistida no projeto**, gravação de keyframes em clips reais, sequências JSON; modelagem/rig/pesos com undo e preservação de atributos, e terreno, como modos do viewport principal; timeline única.
**Por que agora.** Requer assets/clips (F6) e `ViewportModeController` (F5); antes do export para que clips/rigs/terrenos entrem no pacote.

| Caminho | Ação | Nota |
|---|---|---|
| `modules/animation/AnimationModule.ts`, `AnimationStateMachine.ts`, `ecs/components/StateMachine.ts`, `scripting/api/animation.ts` | modify/create | `serialize()/deserialize()` **já existem (`:359-381`, inclusive `blendTrees` Map↔objeto)**; o que falta: `StateMachineComponent` (hoje guarda só a instância, `GameComponents.ts:86-94`) serializar `{typeId, stateMachineAssetId, params}`; `ProjectFile.animations` persistir defs; `animation.setParam/play/crossFade` na `ScriptAPI` |
| `editor/animation/TransformTrackRecorder.ts`, `editor/ui/Timeline.ts`, `editor/commands/KeyframeCommand.ts`, `AnimationEditorPanel.ts`, `CinematicEditorTab.ts`, `panels/AnimStateMachineEditor.ts` | create/modify | keyframes → `THREE.AnimationClip` salvo como asset `clip`; **`Timeline.ts` substitui as três timelines (`EditorTimeline.ts` 1.374 linhas, a de `AnimationEditorPanel` e o canvas de `CinematicEditorTab:148`) — `EditorTimeline` é apagado aqui**; sequências `.sequence.json` tocadas por `CinematicEngine` (DSL Lua só como importador); undo via `KeyframeCommand` |
| `editor/modes/{MeshEditMode,RigMode,TerrainMode}.ts`, `core/geometry/{MeshOps,Topology,Sculpt,Weights}.ts`, `editor/commands/GeometryCommand.ts` | create | plugados no `ViewportModeController`; funções puras extraídas de `ModelingRiggingPanel.ts` (1.920) e brushes de `TerrainEditorPanel.ts`; edição sobre `geometry.clone()` com commit por undo; `subdivide` preservando `uv/normal/skinIndex/skinWeight`; pesos por k ossos mais próximos normalizados; `GLTFExporter` com skin; terreno como componente com heightmap em `assets/terrain`; **os 4 renderers próprios (Modeling/Cinematic/Animation/Terrain) morrem** |
| `project/SceneSerializer.ts` | modify | `SkinnedMesh/Skeleton/Bone` e referências a clips/state machines/terreno |
| `tests/animation/{TransformTrackRecorder,StateMachineComponent.roundtrip,SequencePlayer,KeyframeCommand}.test.ts`, `tests/modeling/{GeometryOps,AutoWeights,GeometryCommand,Skeleton.roundtrip}.test.ts`, `e2e/animation.spec.ts` | create | 2 keyframes → clip interpolado; `Idle→Run` quando `speed > 0.1` após reload do projeto; extrude/inset/subdivide/weld preservam contagens e atributos; pesos somam 1; skeleton round-trip preserva ordem de bones e `bindMatrix`; e2e: importar `rigged-cube.glb` → State Machine → script define `speed` → clip ativo |

**Escopo fechado (define os rótulos).** *Modeling "Parcial"* = seleção vértice/aresta/face, mover/rotacionar/escalar seleção, extrude de face, inset, subdivide (com atributos), weld por distância, apagar face, inverter normais, salvar como asset. *Rig "Beta"* = criar cadeia de ossos, bind, auto-pesos k-nearest, pincel único de peso. *Fora* (README "Planejado"): bevel, loop cut, UV editor, heat-map, IK/retargeting no editor.
**Cortes.** Abas Modeling/Terrain/Animation/Cinematic com renderer próprio; `EditorTimeline.ts` e as outras duas timelines; `subdivideMesh` atual; parser Lua como formato de cinemática; `'river'` só volta se couber no timebox.
**Reuso.** `AnimationStateMachine.ts` (serialize já pronto), `CinematicEngine.ts` (letterbox/legendas), algoritmos do `ModelingRiggingPanel`, `TerrainSystem.ts` (+`erosionWorker`), `UndoManager`, `TransformControls`.
**Verificação.** `npm run check` (animation ≥ 70%, modeling ≥ 70%) e e2e; `tests/arch/single-renderer` limite = **2** (`core/Engine`, `ViewportSurface`); manual: gravar 3 keyframes, salvar, reabrir, Play reproduz; extrude + Ctrl+Z restaura bytes iguais; cilindro com 2 ossos, bind, rodar osso deforma; terreno esculpido e pintado, Play com KCC sobre heightfield; README com a lista fechada acima.
**Riscos.** Modelagem é poço sem fundo — timebox de 2 semanas; serialização de `Skeleton` coberta por round-trip próprio.
**Esforço.** 4 semanas.

### F8 — Runtime empacotado, export HTML/ZIP/single-file e Electron (2 semanas)

**Objetivo.** `blindfake-runtime.js` gerado da própria engine (Vite lib) a partir do `GameRuntime` da F4; pacote (index.html + runtime + project.json + assets + decoders + scripts + **HUD**) sem CDN e sem JSON interpolado; pacote Electron pronto para `npm run dist`.
**Por que agora.** Tudo já é serializável e o Play já usa o `GameRuntime`; é a entrega literal do dono.

| Caminho | Ação | Nota |
|---|---|---|
| `runtime/{bootGame,dev}.ts`, `client/play.html`, `vite.runtime.config.ts` | create | `bootGame(canvas, projectUrlOrData)`; `build.lib` com `formats: ['es','iife']` (o IIFE existe porque `file://` bloqueia módulos ES); saída em `assets/public/runtime/`; `build` = `build:runtime && tsc && vite build`; `/play.html?project=<id>` substitui o 'Exit to Game'; `no-restricted-imports`: `runtime/`/`core/` não importam `editor/` |
| `export/{PackExporter,ExportManifest,ElectronExporter}.ts`, `export/templates/{index.html,main.js,preload.js,package.json}.ts` | create | zip com `index.html` (`<script type="module">import {bootGame} from './runtime/blindfake-runtime.js'`), `project.json` (inclui `ui`), `assets/<hash>.<ext>`, `scripts/<id>.lua`, `decoders/**`; single-file: IIFE inline + JSON em `<script type="application/json">` com `</` → `<\/` + assets data URL; `ExportManifest` lista `asset://` faltantes; Electron reaproveita `generatePackageJson/MainJs/PreloadJs/Readme` de `git show pre-rework:client/src/engine/ElectronExporter.ts`, CSP `default-src 'self'` e `www/` do `PackExporter` |
| `panels/EditorMenuBar.ts`, `editor/dialogs/ExportDialog.ts` | modify/create | comandos `file.export.pack` (Ctrl+E), `.single`, `.electron` no `CommandRegistry` |
| `scripts/export-smoke.mjs`, `tests/{PackExporter,ElectronTemplates}.test.ts`, `e2e/{export.pack,first-game}.spec.ts`, `ci.yml`, `docs/EXPORT.md` | create/modify | nome com `</script>` não quebra o HTML; zip sem `http`; **Electron: teste estático dos templates gerados** (CSP sem CDN, `contextIsolation:true`, `sandbox:true`, `nodeIntegration:false`); `npx electron .` só local com `ELECTRON_SMOKE=1` (documentado como manual); e2e `export.pack`: exporta cubo com script + HUD, descompacta, serve estático, asserta `rotation.y` crescendo, widget do HUD visível, zero `pageerror`, e repete single-file via `file://`; **e2e `first-game`**: novo projeto → Add Cube → Add Component CharacterController → script → export → pacote roda (substitui a métrica humana "≤ 20 min"; orçamento do teste ≤ 3 min no CI); budget ≤ 4 MB gz |

**Determinismo.** Comparação por **contagem de ticks**, não por tempo de parede: `GameRuntime.step(300)` headless duas vezes → estado igual (`toBeCloseTo(…,10)`), já coberto na F4 e re-executado sobre o bundle empacotado no `export-smoke`.
**Cortes.** Qualquer resquício de viewer via CDN; 'Export Scene JSON'.
**Reuso.** `GameRuntime` (F4), `project/zip.ts`, `editor/ui/dialogs`, geradores do `ElectronExporter` original.
**Verificação.** `npm run build:runtime` ≤ 4 MB gz; `npx playwright test export.pack first-game`; manual: exportar platformer → `npx serve` → física, Lua, blueprints, animação e HUD iguais ao Play; Network sem domínios externos; Electron: `npm i && npm run dist` no Windows gera `.exe` (manual, documentado), `typeof process === 'undefined'` no renderer.
**Riscos.** Tamanho (three + rapier wasm + fengari) — `manualChunks` como otimização futura; `electron-builder` exige toolchain do SO alvo.
**Esforço.** 2 semanas.

### F9 — Multiplayer mínimo real, docs geradas e fechamento (3 semanas)

**Objetivo.** Servidor autoritativo simples (salas, tick 20 Hz, `WORLD_STATE`/`PLAYER_INPUT`) usado por um `NetworkModule` e por um lobby no jogo exportado; docs de API geradas; README/ROADMAP/METRICS finais; retirar o `ignore` do knip.
**Por que agora.** Última prioridade do dono; precisa do runtime exportável para testar dois clientes reais.

| Caminho | Ação | Nota |
|---|---|---|
| `server/src/rooms/Room.ts`, `server/src/server.ts`, `shared/protocol.ts`, `shared/types.ts` | create/modify | tick 20 Hz emitindo `EntitySnapshotPayload`, aplica `PLAYER_INPUT` com `GAME_CONFIG.PLAYER_MOVE_SPEED`, ack de sequence; rate limit por socket, origin allowlist, senha de sala; `NetworkMessage.payload` vira união discriminada (`:53` é `any`) |
| `modules/network/{NetworkModule,ReplicationSystem}.ts`, `scripting/api/network.ts`, `modules/ui/Lobby.ts`, `panels/MultiplayerPanel.ts`, `knip.json` | create/modify | embrulha `NetworkManager` (F2); `ReplicatedTransformComponent` interpolando com `NETWORK_INTERPOLATION_DELAY`; `network.isConnected/playerId/send/on`; lobby com chat de sala; `ignore` do knip removido |
| `docs/api/` (typedoc sobre `runtime/index.ts`, `scripting/api`), `docs/LUA_API.md`, `docs/STARTER_KIT.md` (final), `docs/tutorials/{01_first_game,02_scripting}.md`, `tests/docs.test.ts`, README/ROADMAP/METRICS | create/modify | CI falha em warnings do typedoc; teste valida que todo símbolo documentado existe como export, entrada da referência Lua ou `typeId` no `ComponentRegistry`; tutoriais só com passos reproduzidos por `first-game`/`scripting.play` |
| `tests/server/rooms.test.ts`, `tests/modules/ReplicationSystem.test.ts`, `e2e/multiplayer.spec.ts` | create | dois clientes `ws`: `PLAYER_JOIN`, `WORLD_STATE` a 20 Hz; spam desconecta o spammer; dois `BrowserContext` no jogo exportado contra servidor real em porta efêmera |

**Cortes.** `payload: any`; `/api/dev-errors` em produção; promessas de delta compression/interest management.
**Reuso.** `NetworkManager.ts` (+teste da F2, que já cobre as 5 tentativas), `server.ts`/`protocol.ts`, `shared/types.ts`, `UIManager.loadLayout`.
**Verificação.** `npm run check` (server ≥ 80%, network ≥ 70%) e e2e multiplayer; manual: dois browsers, sala com senha, movimento interpolado; `npm run docs:api` sem warnings; `check-readme-claims` final; `knip` = 0 sem `ignore`.
**Riscos.** Física autoritativa fora de escopo (autoridade de posição por input, documentado); e2e com dois clientes pode ser flaky (timeouts generosos, retry 1×).
**Esforço.** 3 semanas.

**Total estimado:** 26-30 semanas.

---

## 3. Lista consolidada de cortes

| Motivo | Arquivos | Importador/teste que morre junto |
|---|---|---|
| Zero importadores (F1) | `2d/{Physics2D,Sprite2D}.ts`, `ai/{Pathfinding,StateMachine,BehaviorTree}.ts`, `engine/{CameraController,EditorEventBus,LightingManager,PerformanceMonitor,ProceduralGeneration,RaycastUtils,VegetationSystem,AssetManifest}.ts`, `gameplay/InventorySystem.ts`, `workers/PhysicsWorker.ts`, `editor/{EditorManager,CinematicEditorPanel,ParticleEditorPanel,ModelInspectorPanel,LayoutPresets,PanelPersistenceService,SceneVersioning,KeybindEditor}.ts` | `EditorKeyboardShortcuts.useKeybindEditor`; rebind volta na F5 (`ShortcutsPanel`) |
| Só testes (F1) | `engine/{UISystem,AudioMixer,AudioEffects,TouchInputManager,VirtualJoystick,Benchmark,SpatialAudioManager,AdaptiveMusicSystem}.ts`, `network/{ClientPrediction,EntityReplicator,RoomManager}.ts` | 10 arquivos de teste (171 casos) |
| Fachada/inviável (F1) | `engine/{WebGPURenderer,GameExporter,ElectronExporter,BuildExportSystem,PluginSystem,HotReload,WorkerManager,AssetPipeline,AssetDependencyGraph,Profiler,LightProbeSystem,OcclusionCulling,SplineSystem}.ts`; opção `'river'` (`TerrainEditorPanel.ts:994`) | `Engine.ts`, `EditorMenuBar.ts:3` + itens de export/'Exit to Game', `PlayModeSystem.ts:12`, seção Plugins, `tests/BuildSystem.test.ts` (10) |
| Construído e nunca renderizado (F1) | `panels/{AssetBrowser,MaterialEditor}.ts`, `editor/MultiSelect.ts`; div `timelinePanel` (`EditorLayoutBuilder.ts:69-72`) | `EditorApp.ts:361-365`, `EditorTabController` (timelinePanel), menu Window→Timeline/Asset Browser. **`EditorTimeline.ts` não** (vivo até F7) |
| Templates imperativos (F1/F4) | 6 de 9 funções de `ProjectTemplates.ts` (F1); o arquivo inteiro vira JSON + Starter Kit (F4) | `tests/ProjectTemplates.test.ts` −7 casos; `ProjectTemplate` de 9 → 3 valores |
| Docs inventadas/corrompidas (F1) | `docs/API.md`, `docs/tutorials/*` | nenhum |
| Sem consumidor no núcleo (F3) | `engine/{PerformanceManager,PrefabManager,SaveManager,PostProcessing,FogSystem}.ts`, `TerrainSystem.generateCollisionMesh` (+ chamadores `ProjectTemplates.ts:209`, `TerrainEditorPanel.ts:977`) | `EngineSystemsPanel` (seções Performance/Prefabs), `Engine.ts:219/224` |
| Scripting fake (F4) | `engine/ScriptingSystem.ts`, `cinematics/LuaScriptRunner.ts` (exceto `LUA_API_REFERENCE`, `EXTENDED_LUA_TEMPLATES`, `trackToLua`), `types/fengari-web.d.ts`, dep `fengari-web`, textarea `EditorInspector.ts:698-716`, `editor/PlayModeSystem.ts` (snapshot) | `ScriptEditorPanel.ts:13-18`, `CinematicEngine.ts:3,54,85`, `tests/{ScriptEditorPanel,PlayModeSystem}.test.ts` adaptados |
| Persistência fora do projeto (F4) | `localStorage['blindfake_ui_layouts']` (`UIEditorPanel.ts:1188/1194`), `blindfake_autosave` global, `LandingPage.sceneData`, HUD DOM dos templates | migração v1 → v2 |
| Substituído por implementação real (F5/F7) | 5 `escapeHtml`, 12 context menus, 32 diálogos nativos, canvas do `AnimStateMachineEditor`, `pushUndo` do grafo, renderers de 4 painéis 3D, `panels/EditorTimeline.ts` + 2 timelines, `subdivideMesh` | `EditorApp.ts:650`, `AnimationEditorPanel.ts:352-353/718-780` (F7) |

---

## 4. Gates de qualidade

- **CI** (Node 20.19 e 22): `typecheck` ×3, `lint`, `test:coverage`, `build`, `build:server`, `check-encoding` (blocking desde F2), `knip` (blocking desde F1), `check-readme-claims` (F1), Playwright (`editor.smoke` desde **F0**; `scripting.play` F4; `project.import` F6; `animation` F7; `export.pack` + `first-game` + budget ≤ 4 MB gz F8; `multiplayer` F9), `export-smoke` (F8), `docs:api` sem warnings (F9). Nenhum gate é removido depois de entrar.
- **Lint:** `no-new-func`/`no-implied-eval` error desde F0; CDN warn → error na F4; `constructor.name`/`type.name` error na F3; `eslint-plugin-no-unsanitized` (`property`/`method`) e `alert/prompt/confirm` error na F5 com `overrides` para `editor/ui/{dom,html}.ts`; `no-restricted-imports` (`runtime/`, `core/` ⇏ `editor/`) na F8.
- **Cobertura (v8, thresholds por glob):** F3 `core ≥ 70`, `ecs ≥ 80`, `modules/physics ≥ 60`, `server ≥ 70`; F4 `scripting ≥ 80`, `project ≥ 85`, `runtime ≥ 70`; F7 `modules/animation ≥ 70`, `core/geometry ≥ 70`; F9 `server ≥ 80`, `modules/network ≥ 70`; global ≥ 40% ao fim.
- **Benches com orçamento (`vitest bench`, regressão > 20% falha):** física 2000 corpos (F3), cloth/navmesh/IK (F3), 200 scripts Lua < 4 ms (F4).
- **Segurança:** HTML com dado de usuário só via `html`/`textContent`; scripts só via `LuaHost` com env allowlist e orçamento; export sem JSON interpolado em JS e CSP `default-src 'self'` no Electron (teste estático); servidor com `maxPayload`, type guards, rate limit, origin allowlist; `/api/dev-errors` e `DevErrorTracker` só em DEV.
- **Arquitetura por teste:** `tests/arch/single-renderer` (15 → 7 F1 → 6 F2 → 6 F5 → 2 F7); contador de contextos WebGL no e2e (Scene = 1 desde F2; 10 abas × 3 aberturas ≤ 6 e 0 `webglcontextlost` na F5); `panels.lifecycle` + `EditorApp.lifecycle` (0 listeners/RAF após dispose); `GraphRuntime` (100% dos nós com `execute`); `EditorInspector.updates` (0 rebuild por transform); `StarterKit` (todo componente documentado existe no registry); `docs.test`.

## 5. Métricas de progresso (`scripts/metrics.mjs` → `docs/METRICS.md`)

**Baseline corrigido (medido nesta rodada):** 78.286 linhas totais = **73.209 produto** (client+server+shared) + **5.077 testes**; 156 arquivos TS em `client/src`; 14 módulos sem importador + 8 órfãos do editor + 11 só-teste; 331 `it()` (345 em runtime) em 36 arquivos, 45 asserções fracas; **224** `innerHTML =`, **567** `cssText =`, 32 `alert/prompt/confirm(`, 679 `addEventListener` vs **72** `removeEventListener`; 7 `constructor.name`; 32 tokens `--ed-*` / 0 usados em TS; 13 injeções de `<style>`; 335 `this.editor.`; 15 `new THREE.WebGLRenderer`; 9 referências a CDN em 5 arquivos; 2 `new Function`; 17 `new *System()` no construtor do Engine; 20 usos de `editorActive`; **11 arquivos com codificação corrompida** (+2 a inspecionar); 0 arquivos de CI/lint.

**Metas:** linhas de **produto** F1 ≤ 53k, F5 ≤ 50k (testes contam à parte e podem crescer); módulos mortos = 0 desde F1; testes reais F1 ≈ 143, F3 ≥ 200, F5 ≥ 300, F9 ≥ 450 com asserções fracas ≤ 5; `innerHTML` fora de `ui/` = 0 e diálogos nativos = 0 na F5; razão add/remove listener ≤ 1,2 na F5; `this.editor.` ≤ 60; `WebGLRenderer` = 6 na F2 e 2 na F7; contextos WebGL na aba Scene = 1 desde F2; CDN = 0 e `new Function` = 0 na F4; `new THREE.*` em hot paths de Cloth/IK só decresce (baseline 20/43); tamanho do runtime ≤ 4 MB gz; cobertura conforme gates; e2e `first-game` ≤ 3 min no CI (F8).

## 6. Perguntas em aberto para o dono

1. **Linguagem de script:** Lua real (fengari, sandbox com orçamento, ~10-30× mais lento que nativo) como host único na F4 — aceita? Adicionar JS ES modules como segundo host (sem sandbox, +1 semana) agora ou depois?
2. **Controllers como componentes (decidido, confirmar):** os player controllers dos templates deixam de ser closures TS e viram componentes do Starter Kit (`CharacterController`, `CameraFollow`) na F4 — ok, ou prefere mantê-los como scripts Lua de exemplo?
3. **Armazenamento:** IndexedDB (todos) na F4 e pasta real via File System Access (Chromium) na F6 — suficiente, ou Firefox/Safari precisam de paridade além do `.bfproject`?
4. **2D:** Rapier 3D com eixos travados + câmera ortográfica (zero dependência nova) ou `@dimforge/rapier2d-compat` como módulo próprio?
5. **EXE:** Electron por `npm run dist` fora do browser (~150 MB) reaproveitando os geradores existentes, ou Tauri (~10 MB, exige Rust)?
6. **Dependências novas:** `fengari` + `fengari-interop` (diretas), `fflate`, `monaco-editor`, `@playwright/test`, `knip`, `typedoc`, `fake-indexeddb`, `eslint-plugin-no-unsanitized`; `zod` não entra. Alguma restrição?
7. **Modelagem/rig:** a lista fechada da F7 (Parcial/Beta) atende, ou prefere investir em importação de rigs prontos antes?
8. **Multiplayer:** autoridade só de posição por input basta, ou quer física autoritativa (Rapier no Node)?
9. **Histórico:** PRs pequenos na branch `claude/lucid-gauss-a1vpbn` com tag `pre-rework` em `caa28c7` — squash por fase ou histórico granular?
10. **Samples:** só platformer-3d, topdown-2d e fps na F6, ou recriar também MainMenu/Fighting/Racing/Puzzle?
11. **Textura (novo):** a visão diz "criar texturas". Manter a pintura do `TextureEditorPanel` como "Parcial" (paint sobre asset com undo, sem camadas/UV unwrap) e evoluir depois, ou congelar nesse nível?
12. **Prefabs / cenas aninhadas (novo):** quer instanciar uma cena ou prefab dentro de outra (Godot/Unity)? Afeta o `ProjectFile` v2 — o campo `prefab?` fica reservado na F4 e o `PrefabManager` atual (sem consumidor) é apagado na F3; a implementação real entraria na F6 (+1 semana).

**Itens da crítica rejeitados:** (a) medir A.10 por "contador de `new` via wrapper em teste" — as classes de `three` são importadas diretamente pelos módulos e não podem ser embrulhadas de forma confiável; substituí por benches com orçamento e contagem estática decrescente no `metrics.mjs`; (b) mover Cloth/IK/NavMesh para "Planejado" no README — eles funcionam (só são lentos), então o rótulo honesto é "Beta" com gate de bench; (c) a alternativa "manter `engine.network` até a F9" — escolhi o `ignore` explícito no knip porque manter o `NetworkManager` ansioso no `Engine` contradiz a composição da F3.